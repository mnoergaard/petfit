% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch_apps.R
\name{petfit_interactive}
\alias{petfit_interactive}
\title{Launch petfit Interactive Apps}
\usage{
petfit_interactive(
  app = c("regiondef", "modelling_plasma", "modelling_ref"),
  bids_dir = NULL,
  derivatives_dir = NULL,
  blood_dir = NULL,
  petfit_output_foldername = "petfit",
  analysis_foldername = "Primary_Analysis",
  config_file = NULL,
  cores = 1L,
  save_logs = FALSE,
  ancillary_analysis_folder = NULL
)
}
\arguments{
\item{app}{Character string specifying which app to launch: "regiondef", "modelling_plasma", or "modelling_ref" (required)}

\item{bids_dir}{Character string path to the BIDS directory (default: NULL)}

\item{derivatives_dir}{Character string path to derivatives directory (default: bids_dir/derivatives if bids_dir provided)}

\item{blood_dir}{Character string path to the blood data directory (default: NULL, for modelling_plasma app)}

\item{petfit_output_foldername}{Character string name for the petfit output folder within derivatives (default: "petfit")}

\item{analysis_foldername}{Character string name for analysis folder (default: "Primary_Analysis", for modelling apps)}

\item{config_file}{Character string path to existing config file (optional, for modelling apps)}

\item{ancillary_analysis_folder}{Character string name of a sibling analysis subfolder to inherit
delay or k2prime estimates from (optional, for modelling apps). Must be a subfolder name
(e.g., "Ancillary_Analysis"), not a full path.}
}
\description{
Launch a specified petfit interactive Shiny application
}
\details{
This function provides a unified interface to launch petfit interactive applications built on kinfitr:
\itemize{
\item "regiondef": Region Definition App for creating brain region definitions and generating combined TACs
\item "modelling_plasma": Plasma Input Modelling App for invasive kinetic models (1TCM, 2TCM, Logan, MA1)
\item "modelling_ref": Reference Tissue Modelling App for non-invasive models and ratios (SRTM, SRTM2, SUVR, refLogan, MRTM1, MRTM2)
}

Parameter usage:
\itemize{
\item regiondef: Uses bids_dir, derivatives_dir, petfit_output_foldername
\item modelling_plasma: Uses bids_dir, derivatives_dir, blood_dir, analysis_foldername, config_file, ancillary_analysis_folder
\item modelling_ref: Uses bids_dir, derivatives_dir, analysis_foldername, config_file, ancillary_analysis_folder
}
}
\examples{
\dontrun{
# Launch region definition app
petfit_interactive(app = "regiondef", bids_dir = "/path/to/bids")

# Launch plasma input modelling app
petfit_interactive(app = "modelling_plasma", bids_dir = "/path/to/bids")

# Launch reference tissue modelling app
petfit_interactive(app = "modelling_ref", bids_dir = "/path/to/bids")
}

}
